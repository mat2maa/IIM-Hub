var $ = jQuery;

var releases = " <%= escape_javascript(render(partial: "releases",
                                             object: @releases)) %> ",
    query = "<%= j params[:last_query] %>";

$('#results').html(releases);

$('#last-query').html(query);

$('.ajax-loader').fadeOut("fast");