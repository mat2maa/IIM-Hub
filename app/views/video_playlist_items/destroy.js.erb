var $ = jQuery;

var $element = $("#item_<%= @videoplaylistitem.id %>");

$element.fadeOut("fast");

$('#bootstrap-flash')
    .html('<div class="alert fade in alert-success">' +
        '<button class="close" data-dismiss="alert">×</button>' +
        'Successfully deleted video from playlist.' +
        '</div>');