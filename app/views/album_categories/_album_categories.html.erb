<table class="table" id="album_categories">
  <thead>
  <tr>
    <td>ID</td>
    <td>Name</td>
    <td>Code</td>
    <td>&nbsp;</td>
  </tr>
  </thead>

  <tbody>
  <%= render partial: 'album_category',
             collection: @album_categories %>
  </tbody>
</table>

<br/>
<br/>
<%= will_paginate @album_categories,
                  inner_window: 1,
                  renderer: BootstrapPagination::Rails,
                  class: 'pagination pagination-small' %>
<%
   def items_per_page_select(collection = AlbumCategories)
     select_tag :per_page, options_for_select([10, 20, 50, 100, 300], collection.per_page),
                class: 'items_per_page_select_menu'
   end
%>
<%= form_for '/album_categories', method: :get, html: {class: 'items_per_page_select'} do |f| %>
  <span>Items per page: </span>
  <%= items_per_page_select @album_categories %>
  <%= f.submit "Submit",
               class: 'btn btn-small' %>
<% end %>
