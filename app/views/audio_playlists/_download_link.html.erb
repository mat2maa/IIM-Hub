<% if @audio_playlist.finished? %>
  <div class="download-info-inner">
    <%= link_to "Download Playlist Zip",
                "#{current_user.current_login_ip == "182.19.135.7" ? Settings.nas_zip_url : Settings.nas_zip_url_remote}audio_playlists/#{@audio_playlist.id.to_s}.zip",
                target: '_blank' %>
  </div>
<% else %>

<% end %>
