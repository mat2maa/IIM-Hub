<table class="table" id="origins">
  <thead>
  <tr>
    <td>ID</td>
    <td>Origin Name</td>
    <td>&nbsp;</td>
  </tr>
  </thead>

  <tbody>
  <%= render partial: 'origin',
             collection: @origins %>
  </tbody>
</table>

<br/>
<br/>
<%= will_paginate @origins,
                  inner_window: 1,
                  renderer: BootstrapPagination::Rails,
                  class: 'pagination pagination-small' %>
<%
   def items_per_page_select(collection = Origins)
     select_tag :per_page, options_for_select([10, 20, 50, 100, 300], collection.per_page),
                class: 'items_per_page_select_menu'
   end
%>
<%= form_for '/origins', method: :get, html: {class: 'items_per_page_select'} do |f| %>
  <span>Items per page: </span>
  <%= items_per_page_select @origins %>
  <%= f.submit "Submit",
               class: 'btn btn-small' %>
<% end %>
